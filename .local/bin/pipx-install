#!/bin/bash
# Install pipx packages tracked in dotfiles

set -e

# pipx packages to install
packages=(
    "poetry==1.2.1"
)

echo "🔧 Installing pipx packages..."

for package in "${packages[@]}"; do
    echo "Installing $package..."
    pipx install "$package"
done

echo "✅ pipx packages installed!"